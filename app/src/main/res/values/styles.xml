<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--启动界面初始背景-->
    <style name="AppTheme.Start">
        <item name="android:background">@drawable/splash_1</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>
    
    <!--TableLayout样式-->
    <style name="MyTableLayoutStyle" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">#ff4f47</item>
        <item name="tabSelectedTextColor">#4d4d4d</item>
        <item name="tabBackground">@android:color/white</item>
        <item name="tabTextAppearance">@style/MyTabLayoutTextAppearance</item>
    </style>

    <!--TabLayout小标题-->
    <style name="MyTabLayoutTextAppearance" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#999999</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!--自定义RatingBar样式-->
    <style name="MyRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/rating</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:maxHeight">28dp</item>
    </style>

</resources>
